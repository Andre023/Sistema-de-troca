; NÃO use este arquivo com "php -c php.ini" — isso desativa as extensões do PHP (mbstring, gd…).
; Para subir o servidor, use: .\serve.ps1
;
; Limites aplicados pelo serve.ps1 via flags -d:
;   upload_max_filesize = 20M
;   post_max_size = 25M
;   memory_limit = 256M
